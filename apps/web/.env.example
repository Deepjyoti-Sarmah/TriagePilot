# Copy to .env.local. All server-only — never import these in client components.
# Empty = app runs in MOCK mode (deterministic demo output, see lib/mock.ts).
# LLM calls happen inside Activepieces (OpenRouter provider), not here.
AP_API_KEY=
AP_PROJECT_ID=
AP_MCP_URL=
GITHUB_PAT=
