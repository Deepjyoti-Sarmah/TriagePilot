# 004 tasks — Web UI

- [ ] `apps/web` scaffolded (Next.js + Zod), `.env.example` lists all keys, no secrets committed
- [ ] `/chat` calls MCP-entry flow with 1 Activepieces issue, renders structured output
- [ ] Repo switch test: same UI triages 1 VibeCode issue (proves multi-repo)
- [ ] `/runs` reads Tables `runs` (or mock fallback with `missing_config` banner when keys absent)
- [ ] Rate limit + input validation live (unknown repo / bad URL shows clean error)
- [ ] Deployed to Vercel preview, URL pasted into `specs/status.yaml` notes
